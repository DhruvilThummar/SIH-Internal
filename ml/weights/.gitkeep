# This directory holds trained model weights and calibration files.
# These files are excluded from git (see .gitignore).
# Download pre-trained weights from the GitHub Releases page (see README.md).
