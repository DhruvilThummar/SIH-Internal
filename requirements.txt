# SignalScope — Python Dependencies
# CPU-only PyTorch — no CUDA required at inference or judging time.
# GPU users: swap the two lines below for the matching CUDA wheel URL.
--extra-index-url https://download.pytorch.org/whl/cpu

torch==2.6.0+cpu
torchvision==0.21.0+cpu

# API
flask>=3.0.0
flask-cors>=4.0.0

# Image handling — >=10.4.0 for Python 3.13 pre-built wheels
Pillow>=10.4.0

# ML evaluation
scikit-learn>=1.3.0
numpy>=1.24.0
matplotlib>=3.7.0

# Utilities (used in evaluate.py for generator CSV parsing)
pandas>=2.0.0
